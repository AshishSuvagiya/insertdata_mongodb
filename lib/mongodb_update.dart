import 'package:flutter/material.dart';

class MongoDbUpdate extends StatelessWidget {
  const MongoDbUpdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Container(
        color: Colors.grey,
        width: 100,
        height: 100,
      ),
    );
  }
}
